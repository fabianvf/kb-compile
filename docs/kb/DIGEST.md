# KB digest

GENERATED by `kb graph --write`. Do not hand-edit.

Every article's identity, edges, size and headline claims. Read this to decide what to open: it is ~1.0k tokens against ~4.1k for the corpus, so guessing wrong here is cheap and guessing wrong there is not.

Then read the article, or one section of it:

    kb read <article>[#SECTION]

Sizes are estimates (4 chars/token). `->` is an outbound edge; `gated by` must be read first.

4 articles, ~4.1k tokens total.

---

## arch-gates

`arch` · ~1.4k tokens

- owns: internal/kb/article.go, internal/kb/checks.go, internal/kb/ratchet.go, internal/kb/fresh.go, hooks/**
- -> arch-links, ref-surface

- **INVARIANT**: `kb fresh` runs the graph pass first and bails if it fails. A freshness
- **INVARIANT**: Ownership is derived once and shared. Both gates use the same
- **INVARIANT**: A ratchet may shrink and never grow. `writeRatchet` refuses a `--write`
- **INVARIANT**: (+2 more, in the article)
- **GOTCHA**: A `## FILES` table is an ownership claim, exactly like a fenced block.
- **GOTCHA**: A `## SEE ALSO` link to the article's own id is not an outbound edge.
- **GOTCHA**: `article_types` is optional and its absence disables `type` entirely. A
- **DECISION**: Freshness state is per file, not a commit stamp. The rejected design was a
- **DECISION**: A conflicted manifest names the situation and rules out the obvious fix.
- **DECISION**: A missing manifest FAILS rather than skipping. The rejected alternative,
- **DECISION**: (+2 more, in the article)

## arch-links

`arch` · ~1.0k tokens

- owns: internal/kb/links.go, internal/kb/reverseindex.go, internal/repo/git.go, contrib/**
- -> arch-gates

- **INVARIANT**: Every language-specific line serves one column. All nine enforcement
- **INVARIANT**: `git ls-files` decides what the repo contains, never the filesystem. A
- **INVARIANT**: A link command's output is validated, not trusted. A script emitting
- **GOTCHA**: A plain stem match needs the same directory or a distinctive stem.
- **GOTCHA**: Package imports name a directory, so the link is to every file in it. Go
- **GOTCHA**: The reverse index is regenerated and compared byte for byte. That is why
- **DECISION**: Prefer `link_commands` over regex adapters. The adapters reimplement each
- **DECISION**: A failing link command is fatal, and so is one that emits nothing. The
- **DECISION**: Ambiguity yields nothing. `underscoreSplit` reconstructs a flattened
- **DECISION**: (+1 more, in the article)

## arch-retrieval

`arch` · ~969 tokens

- owns: internal/kb/digest.go, internal/kb/read.go, internal/kb/eval.go
- -> arch-gates

- **INVARIANT**: The digest is DERIVED, and that is what makes it trustworthy as a routing
- **INVARIANT**: An article that yields no standard claims falls back to listing its
- **GOTCHA**: Real articles use three claim shapes and the parser must handle all of
- **GOTCHA**: The digest header quotes its own size, so it can only be written after the
- **GOTCHA**: `kb eval` measures the carve, not the content. A KB of perfectly-bounded
- **DECISION**: Claims are capped at three per section, 150 characters each. The digest
- **DECISION**: `kb eval` reports recall against a directory baseline, not precision. The
- **DECISION**: Recall is averaged over every choice of entry file. Which file an agent
- **DECISION**: (+2 more, in the article)

## ref-surface

`ref` · ~733 tokens

- owns: internal/config/config.go, cmd/kb/main.go
- -> arch-gates, arch-links

- **INVARIANT**: Failure output is written as instructions, not diagnostics. Not every agent
- **INVARIANT**: Every allowlist entry requires a written reason. `not_repo_paths` and
- **GOTCHA**: `SourceExtensions` defaults to the union of adapter extensions. A test file
- **DECISION**: YAML by default, JSON still accepted, both is an error. The format is an
- **DECISION**: Unknown keys are rejected. A typo'd `covers:` silently owns nothing, which
- **DECISION**: An explicit `exclude_substrings` REPLACES the defaults. Vendored trees are

