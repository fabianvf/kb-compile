# KB digest

GENERATED by `kb graph --write`. Do not hand-edit.

Every article's identity, edges, size and headline claims. Read this to decide what to open: it is ~278 tokens against ~627 for the corpus, so guessing wrong here is cheap and guessing wrong there is not.

Then read the article, or one section of it:

    kb read <article>[#SECTION]

Sizes are estimates (4 chars/token). `->` is an outbound edge; `gated by` must be read first.

4 articles, ~627 tokens total.

---

## arch-api

`arch` · ~197 tokens

- owns: app/api/**
- gated by: arch-core
- -> recipe-add-endpoint

- **GOTCHA**: `app/api/schema.json` is generated and gitignored, so it exists on a dev

## arch-core

`arch` · ~203 tokens

- owns: app/core/**
- -> arch-api

- **INVARIANT**: `run()` clamps exactly once, in `util.clamp`.

## arch-web

`arch` · ~122 tokens

- owns: web/**
- -> arch-api

- **INVARIANT**: `format()` is the only thing that appends a unit.

## recipe-add-endpoint

`recipe` · ~105 tokens



